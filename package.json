{
  "devDependencies": {
    "vitepress": "^1.4.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "start": "vitepress build && vite preview --port 8080 --outDir .vitepress/dist"
  }
}
